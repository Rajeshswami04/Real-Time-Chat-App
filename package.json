
{
  "name": "chat",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend/chatify && npm run build --prefix frontend/chatify && npm run postbuild",
    "postbuild": "cp -r frontend/chatify/dist/* backend/public/", 
    "start": "npm run start --prefix backend",
    "heroku-postbuild": "npm run build"
  },
  "author": "rajesh",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5"
  }
}